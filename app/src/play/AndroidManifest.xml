<?xml version="1.0" encoding="utf-8"?>
<!-- The Google Play build: Play updates the app, so it may not install packages itself. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.REQUEST_INSTALL_PACKAGES"
        tools:node="remove" />

    <application>
        <receiver
            android:name=".net.SelfUpdate$ResultReceiver"
            tools:node="remove" />
    </application>
</manifest>
