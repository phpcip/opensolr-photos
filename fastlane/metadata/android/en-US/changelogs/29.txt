Tag many photos at once. In the selection bar there is now Tag, always available: with photos ticked it tags those, with none it tags every photo the view is showing, and never anything else in your index. The same tag field and suggestions as editing one photo, the tags each already has are kept, and what a photo shows is untouched. Saving closes the sheet and the writing carries on in the background, in batches, so it costs one AI request per fifty photos instead of one per photo.
