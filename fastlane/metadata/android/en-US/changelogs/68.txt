Similar photos, rebuilt. Groups no longer run into one another, so a group is a handful of photos that really are alike instead of half your library. The slider now starts at the same first 2 and first 3 words, and a group of more than 50 photos is left out as the category it is. The view opens and pages in a single request, so it is quick on a big library, and it is called similar photos throughout, since only the last stop can prove a duplicate.
